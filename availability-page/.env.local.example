# Copy to .env.local and fill in.
#
# Both keys are safe to commit to a private repo — the URL is public-facing
# anyway, and the publishable key only has SELECT access against the
# sanitised public_freebusy view (configured via RLS + column-level grants).
# The Mac's service-role key (which CAN write) is stored in macOS Keychain,
# never in this app.

NEXT_PUBLIC_SUPABASE_URL=https://djlijptucgpcxennmkch.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_…
